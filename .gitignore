# Build
bin/
obj/
*.user
*.suo
*.cache
*.dll
*.exe
*.pdb
*.cache
project.lock.json
.DS_Store
*.log
.vs/
*.rsuser
.dotnet-debug-mcp-breakpoints.json

# Заметки агента (хранятся в каталоге решения, не коммитить)
.cascade-ide/

# Локальные inlay-логи (CASCADE_INLAY_DEBUG / TRACE; рядом с .sln, см. InlayLogPaths)
.inlay-logs/

# Локальные артефакты разработки/публикации
**/artifacts/
publish/
publish-debug/
publish-debug-staging/
publish-hotkeyfix/
publish-gh-release/
Data/
.cursor/
.hybrid-codebase-index/

# Локальные скрипты правок ADR (не шипить)
docs/adr/_patch_*.py

# Cursor agent CLI (качается с downloads.cursor.com / снимок реестра ACP)
.tools/cursor-agent-win-x64.zip
.tools/cursor-agent-package/
.tools/acp-registry-snapshot.json

# Редактор AvaloniaEdit: NuGet AIGuiders.AvaloniaEdit*; исходники форка — отдельный клон (не в этом репо).
dist/
externals/avaloniaedit/
