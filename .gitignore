# Build
bin/
obj/
*.user
*.suo
*.cache
*.dll
*.exe
*.pdb
*.cache
project.lock.json
.DS_Store
*.log
.vs/
*.rsuser
.dotnet-debug-mcp-breakpoints.json

# Заметки агента (хранятся в каталоге решения, не коммитить)
.cascade-ide/

# Локальные артефакты разработки/публикации
publish/
publish-debug/
Data/
.cursor/

# Локальные скрипты правок ADR (не шипить)
docs/adr/_patch_*.py

# Cursor agent CLI (качается с downloads.cursor.com / снимок реестра ACP)
.tools/cursor-agent-win-x64.zip
.tools/cursor-agent-package/
.tools/acp-registry-snapshot.json
