<UserControl x:Class="CDP.GlassCockpit.Windows.UiKit.GlassSoftKeyBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <UserControl.Resources>
    <Style x:Key="GlassSoftKey" TargetType="Button">
      <Setter Property="Background" Value="{StaticResource Glass.SoftKeyBg}"/>
      <Setter Property="BorderBrush" Value="{StaticResource Glass.SoftKeyBorder}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Foreground" Value="{StaticResource Glass.SoftKeyFg}"/>
      <Setter Property="FontSize" Value="11"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Padding" Value="12,6"/>
      <Setter Property="MinWidth" Value="72"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Bd"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="3"
                    Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{StaticResource Glass.SoftKeyBgHover}"/>
                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Glass.ElectricDim}"/>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{StaticResource Glass.SoftKeyBgPressed}"/>
                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Glass.Electric}"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Bd" Property="Opacity" Value="0.45"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </UserControl.Resources>
  <StackPanel Orientation="Horizontal">
    <Button x:Name="Key1" Style="{StaticResource GlassSoftKey}" Margin="0,0,6,0" Click="OnKey1Click"/>
    <Button x:Name="Key2" Style="{StaticResource GlassSoftKey}" Margin="0,0,6,0" Click="OnKey2Click"/>
    <Button x:Name="Key3" Style="{StaticResource GlassSoftKey}" Click="OnKey3Click"/>
  </StackPanel>
</UserControl>
