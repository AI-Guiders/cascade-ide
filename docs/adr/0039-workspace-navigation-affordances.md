# ADR 0039: Навигация по workspace — несколько представлений и «текущий файл + связанные»

**Статус:** Proposed  
**Дата:** 2026-04-16  
**Обновлено:** 2026-04-16 — [область языков](#adr0039-language-scope): C# / .NET как north-star, не «IDE на все языки». Ранее: [«Продуктовая метафора»](#adr0039-product-metaphor).  
**Связь:** [0021](0021-pfd-mfd-cockpit-attention-model.md) (зона PFD и якорь «где я»), [0037](0037-pfd-surface-invariants-and-roslyn-enforcement.md) (навигация ≠ строгая PFD-поверхность), [0010](0010-ui-modes-toml-configuration.md) (пресеты UI), [0030](0030-command-ids-hotkeys-and-ui-registry-layers.md) (команды и палитра), [0017](0017-multi-window-workspace-and-agent-surfaces.md) (мультиоконность, `presentation` / размещение поверхностей).

## Контекст

Классическое **дерево файлов** (обозреватель решения) в основном **изоморфно** структуре проекта на диске. На **крупных** репозиториях глубокая иерархия даёт высокий **когнитивный налог**: держать в голове путь, многократно раскрывать узлы, промахиваться мимо нужной ветки. При этом разработчик часто думает не «путь в дереве», а **сущностями**: текущий символ, соседний файл по смыслу, тест к классу, граница слоя.

Cascade IDE позиционируется как **agent-first** кокпит с явной моделью внимания ([0021](0021-pfd-mfd-cockpit-attention-model.md)); **не** обязана копировать паттерн «одно дерево — единственная навигация» из классических IDE. Напротив: предложить **несколько согласованных представлений** workspace и режим **«вокруг активного документа»** — осознанный продуктовый дифференциатор.

<a id="adr0039-language-scope"></a>

### Область языков (north-star)

**Цель ближайших итераций — C# / экосистема .NET**, а не «IDE на все языки мира». Глубина семантики навигации («связанные» файлы, символы, граф) в первую очередь опирается на **Roslyn** и рабочий цикл `.sln` / проектов. UI самой IDE на **Avalonia**; в продуктовом контуре могут быть сценарии **Blazor** и иной .NET-стек — без обязательства **паритета языковых сервисов** с VS/Rider для Python, Java, Go и т.д.

После открытия продукта **дополнительные языки** (редактор, LSP, навигация) возможны и не запрещены — но это **не северная звезда** и не критерий успеха v1. Формулировки в этом ADR про «универсальный» вид или «языки без семантики» читаются в свете **C#-first**: универсальность **не** является обязательством команды на этапе зрелости кокпита и агента.

<a id="adr0039-product-metaphor"></a>

### Продуктовая метафора и целевое направление (обсуждение)

**«Шкаф vs карта боя»:** классическое дерево проекта ближе к **архивному шкафу** (физический адрес на диске), чем к **карте текущего боя**. В голове у разработчика часто живут **логические связи** («сервис — контракт — тесты»), а не путь `src/domain/...`. На больших решениях обозреватель превращается в **длинный скролл** и поиск «иголки в стоге сена» — это аргумент за **альтернативные первичные** представления, а не за отказ от дерева навсегда.

**Граф связей и situational awareness:** целевой образ — **динамический граф релевантного контекста** вокруг активного файла или задачи: узел «где я» и **лучи** к местам, которые работа затрагивает (зависимости, контракты, тесты, схема данных). В терминах кокпита это ближе к **ситуационной осведомлённости** ([0021](0021-pfd-mfd-cockpit-attention-model.md): устойчивый контур внимания), чем к статичной картинке каталога папок. Полная реализация графа всего решения остаётся вне обязательного scope v1 (см. раздел **Не цели** ниже).

**Agent-first и «дерево от цели»:** человек и агент в типичном сценарии мыслят **зависимостями и задачей**, а не вложенностью каталогов. Навигация «**от цели / от активного узла**», а не «от корня диска», выравнивает UX с тем, как уже ведут себя инструменты вроде Cursor — при сохранении честной работы с файлами на диске под капотом.

**PFD vs MFD для вида «шкаф»:** допустима политика: **живой контекст** (связанные узлы, граф, семантическая карта) — в **зоне первичного внимания** ([0021](0021-pfd-mfd-cockpit-attention-model.md) — регион PFD), а **классическое дерево файлов** оставить как **вторичный** инструмент (перемещение файлов, редкий обзор структуры) — в **MFD** или эквиваленте вторичного контура ([0021](0021-pfd-mfd-cockpit-attention-model.md) — MFD). Точное размещение — пресет ([0010](0010-ui-modes-toml-configuration.md)); принцип: **не конкурировать** за один якорь внимания двумя несовместимыми ментальными моделями без явного переключения режима.

**Semantic Map и «грамматика презентации»:** целевой UX-образ — **семантическая карта** (узлы уровня «Payment Logic», «DB Schema», «API Endpoint» со связями), а не обязательно строка пути `src/.../payment.cs`. Привязка к размещению окон и слотов — через существующий контур **`presentation` / пресеты** ([0017](0017-multi-window-workspace-and-agent-surfaces.md), [0021](0021-pfd-mfd-cockpit-attention-model.md)); детали полей и генерации UI — вне этого ADR (живые чертежи `workspace.toml`, UX).

## Решение (принципы)

<a id="adr0039-p1"></a>

### 1. Дерево файлов — не единственный канон

**Правило:** продукт **поддерживает** классический обозреватель решения как знакомый и полезный режим (обзор структуры репозитория, операции с файлами там, где уместно), но **не** закрепляет его как **единственный** способ ответить на вопрос «где я и что рядом со мной в работе».

**Дополняющие** формы навигации (направления реализации; не обязательно всё в одной версии):

- **«Текущий файл и связанные»** — соседи по папке или namespace, partial-классы, пары код/тест по соглашению, недавние документы в том же срезе задачи;
- навигация по **символам** (типы, члены) и **outline** активного документа;
- **несколько представлений** в одном регионе: вкладки или split (дерево | связанные | символы) с **синхронизацией** выбора с активным документом.

<a id="adr0039-p2"></a>

### 2. Связь с зоной PFD и [0037](0037-pfd-surface-invariants-and-roslyn-enforcement.md)

Навигационные панели в регионе **PFD** по-прежнему выполняют роль якоря «где я в работе» ([0021](0021-pfd-mfd-cockpit-attention-model.md); [0037 — глоссарий](0037-pfd-surface-invariants-and-roslyn-enforcement.md#adr0037-glossary-pfd-dual)). Они **не** обязаны быть помечены как **строгая PFD-поверхность**: интерактив (клики, раскрытие узлов) остаётся допустимым. Новые виды навигации **не** ослабляют инварианты **приборов** (EICAS, компактное здоровье workspace, статус агента и т.д.), явно помеченных контрактом [0037](0037-pfd-surface-invariants-and-roslyn-enforcement.md).

<a id="adr0039-p3"></a>

### 3. Пресеты и размещение

Набор и плотность инструментов навигации могут зависеть от **режима UI / пресета** ([0010](0010-ui-modes-toml-configuration.md)): в одном пресете — минимум панелей, в другом — несколько представлений или дублирование части навигации на **втором мониторе** ([0017](0017-multi-window-workspace-and-agent-surfaces.md)). Конкретные макеты — продуктовая настройка; этот ADR фиксирует **принцип**, не макет пиксель-в-пиксель.

## Последствия

- Появляется явное **разрешение** проектировать навигацию как **набор режимов ответа на «где я»**, а не только как копию дерева файлов из других IDE.
- Реализация потребует: модель **связанных файлов** (эвристики, соглашения, при наличии — семантика Roslyn для C#), UX **синхронизации** между представлениями, команды переключения вида ([0030](0030-command-ids-hotkeys-and-ui-registry-layers.md)).
- Онбординг и discoverability ([0027](0027-small-team-focus-vs-public-maturity.md)) могут объяснять пользователю **зачем** несколько видов навигации, а не один.
- Метафора **PFD = карта боя / живой контекст**, **MFD = шкаф по необходимости** задаёт язык для пресетов и маркетинга; реализация по шагам, без обязательства «убрать дерево» в первом релизе.

## Не цели

- Полный **граф зависимостей** всего решения как обязательный экран первой версии.
- Замена **поиска по файлам**, **Go to symbol** и **палитры команд** — ортогональны этому ADR.
- **Паритет навигации и языковых сервисов для всех языков**, как у «полного» polyglot IDE: north-star остаётся **C# / .NET** ([область языков](#adr0039-language-scope)); отдельные языки позже — по продуктовой очереди, не как условие успеха v1.

## Открытые вопросы

1. Минимальный набор представлений для **первой** поставки навигации в Cascade (в рамках **C#-first**).
2. Канон **«связанных»** для **C#** (Roslyn): контракт данных и UX; расширение на другие языки — **отдельная фаза**, вне north-star.
3. Размещение: только регион PFD vs частичное дублирование в MFD / втором окне на больших экранах.
4. **Semantic Map:** уровень абстракции узлов (файл vs тип vs «фича»), объём графа по умолчанию, производительность на очень больших решениях.
5. Согласование с **грамматикой `presentation`**: одни и те же семантические узлы в конфиге пресета vs отдельный слой данных для карты.
