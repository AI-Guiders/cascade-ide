# ADR 0017: Несколько окон приложения (мультиоконность), зоны экрана и поверхности агента

**Статус:** Proposed (черновик для обсуждения; детали реализации и сроки — после согласования)  
**Дата:** 2026-04-05  
**Обновлено:** 2026-04-11 — подтверждения агента при мультиоконности: PFD-поток, палитра и хоткеи ([0030](0030-command-ids-hotkeys-and-ui-registry-layers.md)); 2026-04-08 — один монитор и несколько `TopLevel`; два монитора (PFD+лобовое | MFD); связь с [0021](0021-pfd-mfd-cockpit-attention-model.md), playbook и топологией презентации; отклонённая мотивация «запрет перетаскивания ОС»; отсылка к подразделу «Топология презентации зон» в [0010](0010-ui-modes-toml-configuration.md).  
**Связь:** [0010](0010-ui-modes-toml-configuration.md) (режимы и TOML поверх каркаса окна; **топология презентации зон** — отдельный подраздел в 0010, не смешивать с `attention_zone_panels`), [0012](0012-floating-workspace-chrome.md) (плавающий хром; отдельные окна как одна из механик), [0021](0021-pfd-mfd-cockpit-attention-model.md) (семантика зон PFD / лобовое / MFD — **не** синоним числа окон), [0025](0025-sdk-attention-zones-and-capabilities.md) (зоны в SDK), [`attention-zone-panel-playbook-v1.md`](../design/attention-zone-panel-playbook-v1.md) (зона ↔ панель ↔ топология; в коде — `AttentionLayoutSurfaceKind`), [0016](0016-agent-client-protocol-external-agent.md) (внешний агент по ACP — ортогонально тому, *в скольких окнах* показывается встроенный UI), [0030](0030-command-ids-hotkeys-and-ui-registry-layers.md) (идентификаторы команд, **`Hotkeys/hotkeys.toml`** + пользовательский оверлей, палитра), [0002](0002-debug-human-agent-parity.md) (паритет MCP), [0008](0008-mcp-contracts-and-testable-infrastructure.md) (контракты MCP). UX-метафора зон внимания: [`concept-pfd-mfd-cascade-v1.md`](../ux/concept-pfd-mfd-cascade-v1.md). Режим полигона: `UiModes/Flight.toml`, семья **Flight**.

## Контекст

Сейчас **одно главное окно** (`MainWindow`) задаёт весь видимый кокпит: редактор, дерево, чат, нижний док и т.д. Конфигурация режимов ([0010](0010-ui-modes-toml-configuration.md)) описывает **видимость и метрики поверх фиксированного каркаса**; альтернативная «схема окна» целиком в данных явно отложена.

Потребность: **разнести роли по экрану** без обязательной ужимки всего в одну колонку — в том числе:

- **Несколько физических мониторов** или **один широкий/изогнутый дисплей** с явными зонами (см. концепт PFD/MFD, §6).
- **Чат и тяжёлые панели агента** (история, trace, операции) как кандидаты на **второе окно** или зону, чтобы не конкурировать с редактором за постоянный фокус.
- **Эксперименты с раскладкой** изолированно от стабильных пресетов — режим **Flight** как песочница; мультиоконность логично отрабатывать там первыми (см. §5 `concept-pfd-mfd-cascade-v1.md`).

[0012](0012-floating-workspace-chrome.md) уже допускает **отдельные окна** для части хрома (телеметрия, полоски) и фиксирует, что снимок MCP и действия по UI сейчас завязаны на **один** визуальный корень. Этот ADR **расширяет предмет**: не только плавающий хром, а **продуктовая модель нескольких окон приложения** (какие поверхности, жизненный цикл, состояние, связь с режимами и агентом).

### Один монитор и три семантические зоны ([0021](0021-pfd-mfd-cockpit-attention-model.md))

Три пространственных якоря внимания (PFD, лобовое, MFD) **не требуют** трёх отдельных окон. Текущая реализация — **одно** главное окно, колонки `MainGrid`, топология `AttentionLayoutSurfaceKind.MainWindowDockedGrid` (см. playbook).

**Опциональный** сценарий этого ADR: разнести те же семантики по **нескольким `TopLevel` на одном физическом мониторе** (например очень широкий дисплей) — один процесс, та же карта панель→зона ([`AttentionZonePanelRuntime`](../design/attention-zone-panel-playbook-v1.md)), иная только **геометрия презентации**. Это не обязательная базовая модель и не цель v1 по умолчанию; целесообразность — по продуктовой приоритизации и обратной связи.

**Не** считается аргументом за мультиоконность: «заблокировать перемещение окон **на уровне ОС**». Прилипание, сетка и предсказуемая раскладка достигаются в рамках **одного** `TopLevel` (сплиттеры, пресеты) или программным позиционированием без обещания запрета средствами оболочки; см. [0022](0022-mfd-visual-design-surface-axaml-blazor.md) (второй монитор как геометрия одного окна — ортогонально числу окон).

### Два монитора: первый — PFD + лобовое, второй — MFD

**Пример целевой раскладки** при двух физических экранах: **первый монитор** — контекст workspace и редактор (**PFD** и **лобовое** / forward); **второй монитор** — под регион **MFD** (чат, терминал, Git, trace и т.д. по пресету). Это согласуется с ролью MFD как вторичного, переключаемого осознанно ([0021](0021-pfd-mfd-cockpit-attention-model.md)): основной взгляд и код на первом дисплее, тяжёлые панели — на втором без конкуренции с лобовым за площадь. Не единственная возможная схема; пресеты могут предлагать иные соответствия дисплей↔зона. Реализация (одно окно на два экрана vs отдельное `TopLevel` под MFD) — по итогам проектирования и контракта MCP (п. 7 в разделе «Решение» ниже).

## Решение (принципы; детализация — по итогам обсуждения)

1. **Один процесс, несколько `TopLevel`.** Окна принадлежат одному экземпляру IDE; общее состояние решения, агента, настроек — **единый** граф VM/сервисов за фасадом, без второго «независимого» экземпляра приложения. *Исключения* (если когда-нибудь понадобятся) — отдельное решение и отдельный ADR.

2. **Главный фрейм.** Сохраняется понятие **основного окна** (редактор, решение, главная навигация по умолчанию). Дочерние/вспомогательные окна **не** обязаны дублировать полный кокпит; их состав определяется пресетом режима и/или действием пользователя («вынести чат»). Закрытие главного окна — **политика выхода из приложения** (закрыть все дочерние или запрос подтверждения) — фиксируется в реализации, не оставлять неявным.

3. **Что может жить во втором и следующих окнах (кандидаты, не обязательно всё в v1):** панель чата и связанный MFD-контент агента; компактная PFD-полоса (подтверждения, критичные индикаторы) на отдельном мониторе; выносимые фрагменты хрома в смысле [0012](0012-floating-workspace-chrome.md). **Документы редактора как floating MDI** остаются **вне объёма этого ADR**, пока не будет отдельного решения (как в [0012](0012-floating-workspace-chrome.md)).

4. **Связь с режимами UI ([0010](0010-ui-modes-toml-configuration.md)).** Пресеты задают **видимость и слоты** поверх доступных поверхностей: одно окно или несколько. **Топология презентации** зон (`AttentionLayoutSurfaceKind`: одно окно / несколько `TopLevel` и т.д.) — по мере появления альтернатив в продукте — описывается в данных тем же слоем overlay, что и остальной хром: правила мержа, `workspace.toml`, отделение от карты панель→зона — см. подраздел «Топология презентации зон» в [0010](0010-ui-modes-toml-configuration.md); конкретное имя ключа и схема — после согласования реализации мультиоконности здесь. Расширение: при необходимости ключи/секция уровня «какие панели во вспомогательном окне по умолчанию» — **в TOML режима** или во **Flight**-специфичном файле, без дублирования глобальной схемы `MainGrid` в каждом режиме там, где достаточно флагов.

5. **Сохранение геометрии.** Позиции и размеры вспомогательных окон — **не** обратная запись в шипнутые `UiModes/*.toml` (правило [0010](0010-ui-modes-toml-configuration.md)); целевой канал — **пользовательские настройки** / отдельный блок состояния workspace (как только появится договорённость с существующим `settings.toml` и `workspace.toml`).

6. **Агент (ACP, [0016](0016-agent-client-protocol-external-agent.md)).** Транспорт к внешнему агенту **не зависит** от числа окон: один канал stdio/сессия. Решение этого ADR — только **куда монтируется** встроенный UI ответов и подтверждений (главное окно vs дополнительное). Краткие подтверждения и критичные сигналы по-прежнему должны быть доступны в **PFD-зоне** внимания (концепт §3); при втором окне — либо дубль компактной полосы, либо явное правило «фокус на главном окне для подтверждения».

   **Подтверждения при нескольких `TopLevel` (принятое направление):** не опираться только на системную модалку поверх «не того» окна. Показ запроса — в **потоке внимания PFD** (баннер/строка; презентация может быть без классической модалки на весь экран). Опционально **вторичный канал внимания** (звук, мигание в области уведомлений ОС), если фокус на MFD/втором окне. **Ответ пользователя** — те же семантики, что и у кнопок: **палитра команд** и **жесты из [0030](0030-command-ids-hotkeys-and-ui-registry-layers.md)** — шипнутый [`Hotkeys/hotkeys.toml`](../../Hotkeys/hotkeys.toml) и пользовательский оверлей `%LocalAppData%\CascadeIDE\hotkeys.toml` ([0028](0028-user-settings-toml-localappdata-and-secrets.md)). До отдельного решения по протоколу: один вызов `request_confirmation` (и аналоги) по-прежнему завершается **ok/cancel** после ответа; «неблокирующее» относится к **UX презентации**, не к обязательной смене модели MCP.

7. **MCP и паритет ([0002](0002-debug-human-agent-parity.md), [0008](0008-mcp-contracts-and-testable-infrastructure.md)).** Как только в продукте появляется значимый UI **вне** дерева `MainWindow`, **расширение контракта** снимка лэйаута и действий (несколько корней `TopLevel`, идентификация окна/региона) входит **в scope** поставки вместе с фичей — по аналогии с [0012](0012-floating-workspace-chrome.md). До согласования контракта мультиоконные поверхности не должны быть «только для человека».

## Последствия

- Потребуется **дизайн владения VM**: общие сервисы, дочерние окна как представления или отдельные лёгкие VM с подпиской на общий сессионный слой.
- Тесты UI и сценарии с **одним корнем** потребуют обновления или двойного режима (одно окно / два окна).
- Документация для пользователя (позже): как вынести панель, как вернуть, где сохраняется геометрия.

## Отклонённые альтернативы (как целевое состояние)

- **Полагаться только на внешний Cursor/IDE на втором мониторе** без встроенной мультиоконности — отклонено как не закрывающее сценарий «одна Cascade + зоны экрана» и PFD/MFD внутри продукта (см. концепт §8 — внешние инструменты **дополняют**, не заменяют осмысленную раскладку Cascade).
- **Сразу вводить полный MDI документов** — вне объёма; см. [0012](0012-floating-workspace-chrome.md).
- **Несколько окон на одном мониторе ради «запрета перетаскивания средствами ОС»** — отклонено как мотивация: такая цель слаба и закрывается раскладкой **внутри одного окна** (см. подраздел «Один монитор и три семантические зоны» выше).

## Открытые вопросы (для обсуждения перед кодом)

- Нужен ли когда-либо **режим «три связанных окна на одном мониторе»** (те же три якоря, три `TopLevel`) помимо одного `MainGrid` — или достаточно мультиоконности в связке «главное + вспомогательное» и мультимонитора по [0021 §13](0021-pfd-mfd-cockpit-attention-model.md). *См. также (другая ось — один `TopLevel`, мотив «разнести зоны по площади» без мультиоконности):* в [0021](0021-pfd-mfd-cockpit-attention-model.md) подраздел *«Редактор раскладки зон (визуальный, по мотивам FancyZones)»* — пресет/сетка внутри главного окна; там же отсылки к этому ADR и пометка, что отдельный ADR под редактор не вводился до смены инвариантов зон.
- Минимальный **v1**: одно вспомогательное окно только под чат или также под нижний док / телеметрию?
- **Подтверждения агента (детали продукта):** таймауты, деструктивные операции, нужен ли отдельный модальный слой процесса — при реализации поверх принятого направления (п. 6 выше).
- Нужна ли **привязка пресета** к конфигурации мониторов (имя/ид дисплея) или достаточно сохранённых прямоугольников.
- Синхронизация с **Flight**: все эксперименты мультиоконности только в Flight до стабилизации — или допускается Power с флагом.

## Статус после принятия

После согласования черновика: статус **Accepted**, уточнение минимального v1 и при необходимости ссылка из [architecture-policy.md](../architecture-policy.md) и обновление [concept-to-implementation-map-v1.md](../ux/concept-to-implementation-map-v1.md).
