namespace CascadeIDE.Models.Shell;

/// <summary>Одна строка компактного оверлея CascadeChord (alias → краткий заголовок команды).</summary>
public sealed record CascadeChordOverlaySuggestion(string Alias, string Title);
