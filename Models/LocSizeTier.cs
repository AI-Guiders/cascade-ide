namespace CascadeIDE.Models;

/// <summary>Ось размера файла по LOC (непустые строки): Low / Medium / High — не путать с EICAS W/C/A.</summary>
public enum LocSizeTier
{
    Low,
    Medium,
    High
}
