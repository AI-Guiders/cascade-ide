# Dig+ship: glass_scene cabin SA · 2026-08-04

**Leaf:** Dig glass_scene cabin SA pulse gap 2.2
**DoD:** agent omnibus of what Glass paints — without PNG / PrintWindow ritual.

## Dig findings
- `glass_scene` already existed as Citizen @intent compound → `cdp_glass` / `surface_desk` `op=scene`.
- Scene had `shared_ssot` (Plan WHY/NEXT + file_situ) but **not** full cabin (seats/mfd/topology/ignite/alert/shared).
- Webcam latch is agent→human chrome (opposite direction). `cdp_see` = PNG escape, not cabin SA.
- Same latches Glass paints: `plan|seats|presentation|land|shared|ignite|alert-LATEST.json`.

## Ship
- `IdeGlassSurfaceChannel.Scene` → `cabin` (`cabin_sa/v0`) omnibus + `go_alias=glass_scene` pulse.
- Desk go aliases: `glass_scene` · `cabin_sa`.
- Tests: `Scene_includes_cabin_sa_omnibus`.
- Compat: `shared_ssot` retained.

## Residual (not this leaf)
- Live Glass RPC layout tree still separate (surface debt).
- Gap 2.1 PNG ritual shrinks but evidence shots for #CIDE human face still use Capture-Window when claiming visual Done.
- Gap 3.2 Intercom Radio residual · gap 5.4 Halt/talk Autoi visibility.

## Refuse
SoftFL invent · board-hygiene · webcam-as-SA.
